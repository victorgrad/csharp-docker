{
  "name": "BMX-portal-frontend",
  "private": true,
  "version": "3.0.0",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "test": "vitest",
    "coverage": "vitest run --coverage",
    "lint": "eslint 'src/**/*.{ts,tsx}'",
    "preview": "vite preview"
  },
  "dependencies": {
    
  },
  "devDependencies": {
    
  }
}
